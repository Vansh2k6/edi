{
  "name": "@pv/dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/check-bundle.mjs"
  },
  "dependencies": {
    "@pv/schemas": "*",
    "@pv/ui": "*"
  },
  "devDependencies": {
    "esbuild": "*"
  }
}
